{
  "name": "chesslab",
  "version": "2.6.1",
  "private": true,
  "description": "A local-first chess opening trainer and game-review app.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check-syntax.js"
  },
  "devDependencies": {
    "chess.js": "0.10.3",
    "jsdom": "^26.1.0"
  }
}
