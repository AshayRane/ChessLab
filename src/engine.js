// UCI worker lifecycle, generation guards, and analysis queue.
// Kept dependency-injected so browser and Node harnesses can exercise it.
if (typeof module !== 'undefined' && module.exports) module.exports = {};
