// Core domain state and persistence boundary.
// The browser build exposes these declarations through the single-file bootstrap.
if (typeof module !== 'undefined' && module.exports) module.exports = {};
